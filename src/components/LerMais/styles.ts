import styled from 'styled-components';

export const Container = styled.div`
  color: var(--text-body);
  
  button {
    font-size: 1rem;
    padding-left: .5rem;
    background-color: transparent;
    font-weight: 500;
    cursor: pointer;
    border: none;
    color: var(--shape);

  }
`;
